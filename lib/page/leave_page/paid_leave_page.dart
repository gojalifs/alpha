import 'package:flutter/material.dart';

class PaidLeavePage extends StatelessWidget {
  const PaidLeavePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Form Cuti"),
      ),
      body: Center(
        child: Image.asset('images/UnderConstruct.jpg'),
      ),
    );
  }
}
